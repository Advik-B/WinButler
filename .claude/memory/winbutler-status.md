---
name: winbutler-status
description: "WinButler app — what's built (v1 cleaner) and what's next (Phase 2 redirection)"
metadata: 
  node_type: memory
  type: project
  originSessionId: d5219a33-a7fd-42f2-b545-c3f720bf2b9a
---

WinButler is an Avalonia 12 / .NET 10 / CommunityToolkit.Mvvm Windows disk cleaner. Motivation: the C drive is critically full (~16.8 GB free of ~237 GB; D: and S: have far more).

**v1 — DONE (2026-06-30):** scan + dashboard + dry-run + three cleaners.
- `Services/`: `IScanner` (+ `ElectronLeftoverScanner`, `TempScanner`, `CacheScanner`), `ICleaner`/`Cleaner` (dry-run chokepoint + hybrid delete), `DirectorySizeCalculator`, `RecycleBin` (SHFileOperation P/Invoke), `SizeFormatter`.
- Dry-run defaults ON and is a true no-op (returns before any FS mutation). Hybrid delete: `RiskLevel.Safe` → permanent; `Caution`/`Risky` → Recycle Bin.
- Electron detector keys on `app-*` + sibling `Update.exe` and numeric `System.Version` compare (NOT bare version dirs — that would nuke Chrome/Edge component dirs). Verified: flags GitHubDesktop app-3.5.11/3.5.12, keeps app-3.6.1.
- `app.manifest` requests `requireAdministrator`. User runs Claude Code as admin (no UAC babysitting needed).

**v2 — DONE (2026-06-30):** FluentAvalonia UI, comprehensive SafeCaches, Phase 2 redirection.
- **FluentAvalonia** `FluentAvaloniaUI` 3.0.0 (needs Avalonia ≥12). NOTE: v3 prefixed ALL controls with `FA` (`FANavigationView`, `FAInfoBar`+`FAInfoBarSeverity`, `FAComboBox`, `FASettingsExpander`, `FAFrame`); no `ProgressRing`. Theme = `FluentAvalonia.Styling.FluentAvaloniaTheme`. Shell = `MainWindow` FANavigationView (Clean/Redirect pages) + code-behind `OnNavSelectionChanged` using `e.SelectedItemContainer`. Pages resolved via existing `ViewLocator` (so views are `Views.CleanPageView`/`RedirectPageView` to match `*PageViewModel`). Shared `Services/AppSettings` holds `IsDryRun`/`TargetDrive`.
- **`Services/SafeCaches.cs`** — curated catalog; `CacheScanner` delegates to it (`MaxDepth` 5→6). Most-careful-wins: always-safe only for unambiguous Chromium/GPU artifacts; generic `Cache` safe only when anchored to a known context; `Pub\Cache`/`CacheStorage`/`blob_storage` = Caution; denylist `.ssh`/`IndexedDB`/`Login Data`. Verified 9/9 classify cases.
- **Redirection**: `Services/Junction.cs` (real junction via `DeviceIoControl`+`FSCTL_SET_REPARSE_POINT`, `\??\` prefix; detect via `Directory.ResolveLinkTarget`; `Remove` refuses non-junctions). `RedirectionService` ordering validate→copy(robocopy)→verify(file count+bytes)→delete→junction→ledger; Undo reverses. Ledger JSON at `%APPDATA%\WinButler\redirects.json`. Verified 8/8 junction round-trip (C→S). NOT YET tested on a real large dev folder — user should run via GUI (dry-run first).
- **Comprehensive redirect catalog (2026-06-30):** `RedirectionService.Catalog` expanded to ~57 entries with a `Category` field (Build tools / Toolchains / Node.js / Python / ML caches / IDEs / Web tooling / Apps / Games / Misc dev), shown as a tag in `RedirectPageView`. On this machine: 39 existing candidates, 55.3 GB redirectable. Already-redirected junctions correctly detected & excluded.
- **User already manually redirects** `.gradle`, `.nuget`, `go` → `S:\CDriveJunction\<name>` (pre-existing junctions, not made by WinButler). C: is chronically space-starved; S: is the redirect target drive.
- **Externalized path rules (2026-06-30):** all cache rules + redirect catalog moved out of code into `Data/definitions.json` (embedded resource = single source of truth; edit JSON, no recompile). Model `Models/RuleDefinitions.cs` (`WinButlerDefinitions`/`CacheRuleSet`/`RedirectRuleSet`/`RedirectEntry` + `Merge`). `Services/Definitions/`: `IDefinitionSource`, `BundledDefinitionSource` (sync `Load()`), `RemoteDefinitionSource(url)` (HTTP/GitHub, off by default — `provider.AddSource(...)` + `RefreshAsync()`), `DefinitionsProvider` (merges sources, later overrides earlier; redirects keyed by TargetName, cache lists unioned). `SafeCaches` & `RedirectionService` are now instance classes built from the ruleset (both keep parameterless convenience ctors loading bundled). `RedirectCandidate` carries `TargetName`. `MainWindowViewModel` exposes `DefinitionsProvider`.
- **Test project (2026-06-30):** `Tests/WinButler.Tests.csproj` (xUnit), added to `WinButler.slnx` (note: .NET 10 created `.slnx`, not `.sln`). App csproj excludes `Tests/**` from compilation. 43 tests pass (38 original + 5 MFT). Run: `dotnet test Tests\WinButler.Tests.csproj`. NOTE: the suite takes ~2 min because `ScanCandidates` sizes the real profile AND the MFT tests scan real C:.

**v3 — DONE (2026-06-30): "Disk Usage" tab — a real WizTree-like MFT scanner.**
- **`Services/Mft/`** (all `Span`/`BinaryPrimitives`, no `AllowUnsafeBlocks`): `NtfsNative` (raw `\\.\C:` handle, `FSCTL_GET_NTFS_VOLUME_DATA`, cluster-aligned `ReadFile`; mirrors `Junction.cs` P/Invoke style). `MftReader` reads the whole `$MFT` and parses every FILE record — USA fix-up (mandatory), `$FILE_NAME`/`$STANDARD_INFORMATION`/`$DATA` attributes, data-run decoder for record 0's own location. Output = flat `MftEntry[]` indexed by record#. `MftTreeBuilder` → aggregated `DiskNode` tree. `RecursiveWalkScanner` = non-NTFS fallback (same `DiskNode` shape). `DiskScanService` = façade (MFT for NTFS roots; re-roots tree at a subfolder; walk fallback; `GetScannableDrives()`).
- **Three correctness fixes found via headless cross-check vs a recursive walk (which == PowerShell `Measure-Object Length` byte-for-byte):** (1) skip extension records (`BaseFileRecordSegment != 0`) or fragmented files double-count; (2) **aggregation must propagate each node's INTRINSIC size from `entries[i].RealSize`, NOT the mutating `n.SizeBytes`** — a dir's SizeBytes has already accumulated children by climb time → 2.9 GB phantom over-count; (3) recover `$ATTRIBUTE_LIST`-evicted `$DATA` (VCN-0 size from the extension record, credited to base) or big fragmented files (Edge installers) show 0 bytes. After fixes: every shared path matches the walk EXACTLY; the only diff is hardlink duplicate-names. **Hardlinks are counted once per MFT record (per-record), so totals read a bit below WizTree's per-name counting — documented, by design.** Verified live: C: = 216.5 GB / 1.4M files.
- **UI is a `ListBox`, NOT TreeDataGrid.** `Avalonia.Controls.TreeDataGrid` 11.x AND 12.x are now flagged **commercial** by Avalonia 12's licensing MSBuild targets (`AVLIC0001`) → unusable without a paid key. Pivoted to a free, virtualized `ListBox` of flattened visible rows (`ViewModels/DiskRowViewModel` w/ Depth indent + ▸/▾ expander; `DiskScannerPageViewModel` manages expand/collapse splice + Size/Allocated/Name sort) — this is how WizTree itself renders, keeps numeric columns aligned. Plus `Controls/TreemapControl` (custom `Control.Render`, squarified treemap, FNV-hash hue, hover tooltip, click→`NodeInvoked`→re-root). Page wired as nav `Tag="disk"` + `MainWindowViewModel.DiskPage`.

**v4 — DONE (2026-07-01): "Duly Doted" full UI reskin + Dev Junk feature + Dashboard, from a Claude Design mockup.**
- **Full FluentAvaloniaUI removal.** Replaced with custom `ControlTheme`s in `Themes/` (Button/CheckBox/ToggleSwitch/ComboBox/Menu/ScrollBar/ListBoxItem/ProgressBar/NavButton) over base Avalonia `FluentTheme` (kept only for stock primitives). True-black canvas, embedded fonts (Aldrich/BitcountSingle/Doto/Geo/PixelifySans/PressStart2P via `avares://`), swappable LED accent (red/green). Restructured nav from 3 items (Clean/Redirect/Disk Usage) to 7 (Dashboard/Electron Leftovers/Temp Files/Cache Sweep/Dev Junk/Redirect to Drive/Disk Explorer).
- **Accent swap gotcha — see [[avalonia-resource-lookup-gotcha]].** `ThemeService.Apply()` originally called `Application.Current.Resources.TryGetResource(...)` which silently misses resources defined in `Styles`-merged dictionaries (only searches the bare `Resources` object, doesn't cascade into `Application.Styles`). Fixed by calling `TryGetResource` on `Application.Current` itself. Only caught via live toggle-and-screenshot, not by auditing XAML bindings (grep for `StaticResource` misuse found nothing — the bug was in the C# lookup, not the binding layer).
- **New `Services/DevJunkAggregator.cs`** — Dev Junk screen backend. Reuses `RedirectionService`'s existing candidate catalog (filtered to dev categories) for discovery/on-disk size + `SafeCaches.Classify` via its own shallow (`MaxDepth=3`) walk for the reclaimable subset (deliberately not routed through `CacheScanner`, which hardcodes AppData roots). `IsLocked` = has a `.git` folder. **Perf lesson:** `BuildAsync` takes the already-scanned `IReadOnlyList<RedirectCandidate>` as a parameter rather than calling `ScanCandidatesAsync()` itself — an early version re-scanned from scratch (duplicating an expensive full-tree size pass over ~46 dev-tool roots including things like Android SDK/JetBrains, doubling a scan that already took ~1-2 min on this machine). `DevJunkPageViewModel.ScanAsync` reuses `RedirectPageViewModel.Candidates` instead. Verified end-to-end with real data: 18.43 GB reclaimable across 31 tools; vcpkg correctly flagged 🔒 PROTECTED (real git repo on this machine).
- **New `DashboardPageViewModel`** — aggregates existing pages' totals (no new scanning); disk-hero bar simplified to Reclaimable/Other-used/Free (not a full System/Apps/Media breakdown, which would need its own slow directory walk for a cosmetic number).
- All 50 tests pass (45 original + 5 new `DevJunkAggregatorTests`).

**Tooling — DONE (2026-07-02): in-repo UI harness + CLAUDE.md.**
- **`tools/ui-harness/`** — the PowerShell UI-verification loop, migrated out of the throwaway session scratchpad into the repo and parameterized: `shoot.ps1` (launch/reuse WinButler, pin window to screen (40,40) at 1320×900, `PrintWindow` capture; exe resolved relative to `$PSScriptRoot`, `-OutDir` defaults to `./out`), `uia.ps1` (UI Automation: `dump`/`invoke`/`invoketext`/`checkall`), `click.ps1` (window-relative click), `crop.ps1` (consolidated from 3 hardcoded variants → `-In/-X/-Y/-W/-H/-Scale/-Out`). Captures go to gitignored `tools/ui-harness/out/`. There is still no automated UI test suite — this is the manual build→run→screenshot→compare loop. See `tools/ui-harness/README.md`.
- **`CLAUDE.md`** (repo root) — dev/agent quick reference: build/run/test commands (note UAC self-elevation + ~90s real-I/O test suite), architecture map, and the conventions/gotchas (definitions.json single-source, dry-run chokepoint, deny-list, Tests/ build exclusion + InternalsVisibleTo, the [[avalonia-resource-lookup-gotcha]]).

**Headless UI test suite — DONE (2026-07-02): answered "why not use Avalonia's automated tests?"**
- Avalonia *does* have `Avalonia.Headless` (+ `Avalonia.Headless.XUnit`) — boots the real `App` on a windowless backend, `[AvaloniaFact]` bodies run on the UI thread. It's a **complement, not a replacement** for `tools/ui-harness/`: headless is a different renderer than the on-screen compositor (can't verify design fidelity), and the real elevated end-to-end app (MFT `$MFT` read + junctions) can't run headless. Chosen scope: **interaction/logic only, keep both.**
- **xunit v2 → v3 migration required.** `Avalonia.Headless.XUnit` 12.x depends on `xunit.v3.extensibility.core`, which collides with xunit v2 (`FactAttribute` ambiguous across `xunit.core` + `xunit.v3.core`). Fix: `Tests/WinButler.Tests.csproj` now references `xunit.v3` 3.2.2 + `xunit.runner.visualstudio` 3.1.0 (dropped `xunit` 2.9.2). One code change: `ITestOutputHelper` moved from `Xunit.Abstractions` → `Xunit` (`MftReaderTests`). Added `<NoWarn>xUnit1051</NoWarn>` (v3 analyzer flags token-less I/O overloads the service tests use on purpose).
- **`Tests/Headless/`** (19 tests): `TestApp.cs` (`[assembly: AvaloniaTestApplication]` + headless `AppBuilder`, no Skia), `Fakes.cs` (`FakeScanner`/`FakeCleaner`, `CleanupTarget` builders, `MessengerIsolatedTest` base), `CleanPageTests`/`DashboardTests`/`ShellTests`. **Testable-surface split:** categories are populated directly via public `CategoryViewModel.SetItems` — never `ScanCommand`, which forces a real MFT read through the concrete `sealed DiskIndexService` (no seam to pre-seed; an `IDiskIndex` interface is the deferred opt-in to cover `ScanCommand`/`RescanAll`). **Flakiness fixes:** reset the static `WeakReferenceMessenger.Default` per test (`DashboardPageViewModel` registers in its ctor); pump `Dispatcher.UIThread.RunJobs()` before asserting on `Dispatcher.Post`ed state; assert toast *state transition*, not the 3.6s `DispatcherTimer` auto-dismiss.
- All 69 tests pass (50 prior + 19 headless). Headless tests touch no disk / need no admin (~0.3s); the full suite still takes a few min because the service/MFT tests hit real I/O.

**v5 — DONE (2026-07-03): production-polish pass (19 commits, 70 files, +3690/-594, 149 tests green).** Four phases:
- **Crash-protection:** `Services/Log.cs` (append-only `%APPDATA%\WinButler\logs\winbutler.log`, rotates once at 2 MB, never throws; logs every destructive action). `ViewModelBase.RunGuardedAsync` wraps EVERY async command (CommunityToolkit `AsyncRelayCommand` rethrows on the UI thread → was a hard crash; only DiskScanner had a catch). `Program.cs` AppDomain/TaskScheduler backstops + `App` startup guard/error-window. Re-entrancy gating on RescanAll/CleanAll/Undo (were double-fireable, mutating shared collections); sticky-`SelectedFolder` fix.
- **MFT hardening:** `ApplyUsaFixup`→bool with real bounds (fixed a confirmed `usaCount>513 → rec[-2]` OOB); overflow-safe attr-length checks (`attrLen > rec.Length - pos`, the naive `pos+attrLen` overflowed int — caught by the fuzz suite first run); per-attr-type min lengths; per-record try/catch + `SkippedRecords` + threshold fallback (one bad record no longer degrades the whole volume silently); `MftTreeBuilder` O(N) cycle-break (2-cycles previously spun the 1M climb-guard = hang). `Tests/MftFuzzTests` (synthetic FILE records, byte-flip fuzz) + `MftTreeBuilderTests`.
- **Cancellation (was ZERO CTS in codebase):** per-page `_opCts` + CANCEL buttons; `DiskIndexService` per-build CTS, `Invalidate` cancels the doomed build (fixed the double concurrent MFT read: Invalidate→rescan left the old raw read running); joiners use `Task.WaitAsync(callerCt)`. `IRedirectionService` gained `ct` params (safe checkpoints only, never between delete-original and junction+ledger). `DiskScanService.ScanAsync` is `virtual` (test seam → `FakeDiskScanService`).
- **Redirect/Cleaner safety:** `RecycleBin.Send` reparse-point guard (junction → unlink only, target intact — like `Cleaner.DeletePermanently`); ledger atomic write (tmp+`File.Replace`) + `.corrupt` preservation + `FindOrphanedRedirects` reconciliation; `RunRobocopy` event-driven pipe reads (was a sequential-ReadToEnd deadlock) + ct-kill; honest junction-failure rollback message. Live redirect→undo round-trip test (was untested).
- **Perf (elevated probe, real C: 1.85M records):** DiskNode drops per-leaf `List` + stored file `FullPath` (files compute from `Parent`) → MftTreeBuilder **1230 MB → 448 MB alloc**; walk paths use `DirectoryInfo.EnumerateFileSystemInfos` (killed the double-stat) → **~4× faster**; `RecursiveWalkScanner` de-recursed (explicit stack); `DiskScannerPageViewModel.Sort` now sorts a **projection** (was mutating the shared `DriveIndex` tree — violated its documented immutability). `Tests/PerfProbeTests`.
- **UI/UX:** loading/empty/error states on every page (Electron/Temp/Cache had a dead first-visit screen — now a SCAN prompt); shell subscribes to `CleanupCompletedMessage` → toast (was built since v4 but never invoked in production); `:focus-visible` rings on all controls (custom themes had no FocusAdorner → keyboard focus invisible); treemap theme-aware + keyboard-navigable; converters resolve tokens via `ThemeService.Brush` (was duplicated hex); `WbScrimColor` token.
- **Product hardening:** **confirm dialog wired into every real destructive action** (`ViewModelBase.ConfirmInteraction`, shell TCS over `RequestConfirm`; unset⇒auto-confirm so headless tests unaffected; dry-run never prompts; `ConfirmRequest.ForDeletion` spells the permanent/recycle split). `Services/SettingsStore.cs` persists accent+TargetDrive to `settings.json` — **never `IsDryRun`** (resets ON every launch). Definitions **fail-closed**: `DefinitionsProvider.LoadFailed` → shell builds ZERO scanners + persistent banner (empty ruleset = empty deny-list, must not scan). Deny-list now enforced on Temp + Electron scanners (were bypassing `IsDenied`). Dead `CleanPageView` deleted.
- **Verified elevated end-to-end:** confirm modal both ways (CANCEL nothing-deleted + responsive; CONFIRM real permanent delete of a sacrificial temp `.etl`, logged, responsive, post-clean rescan); live accent swap red↔green; settings persist across relaunch with dry-run back ON.
- **Known finding (NOT fixed — pre-existing, flagged):** the per-category pages (Electron/Temp/Cache) share ONE `CleanPageViewModel`, so `CleanSelected` deletes preselected Safe items across ALL three categories while each page's "N selected" label shows only its own category. The confirm modal now surfaces the TRUE total (e.g. "Delete 1136 item(s)?" when the Temp page shows "1 selected") — a real mitigation — but scoping per-page clean to its own category is a delete-routing behavior change left for the user to decide.

**v6 — DONE (2026-07-06): absorbed a community Windows-cleanup batch script (~1850-line "Wolf cleanup v54") into 4 native features. 189 tests green (was 149).** Five phases:
- **Definitions split:** `Data/definitions.json` → `Data/definitions/` folder of per-domain files (`cache.json`, `redirect.json`, `apps/browsers/drivers/launchers/games/windows.json`), each a partial `WinButlerDefinitions` embedded via `<EmbeddedResource Include="Data\definitions\*.json"/>` and folded at load (`BundledDefinitionSource.Load` sorts by name, merges via `WinButlerDefinitions.Merge`). **Fail-closed is all-or-nothing across the folder** — any one file unparseable ⇒ `LoadFailed` (a lost `cache.json` = a lost deny-list). Contributor `Data/definitions/README.md`.
- **Known-locations catalog + `KnownLocationsScanner` (CleanupCategory.Apps, "App & Game Leftovers" page):** new `knownLocations` section (~55 entries) + `KnownLocationEntry` model (id/path/mode/pattern/recursive/allDrives/risk/group). Modes: `self` (delete the path), `children` (each child, like TempScanner — lock-resilient), `files` (pattern match, reparse-safe recursion). Token expansion: custom `%Documents%`/`%LocalLow%` then `Environment.ExpandEnvironmentVariables`. **Deliberately curated to COMPLEMENT `CacheScanner`, not duplicate it** — excludes any `*cache*`-named folder under LocalAppData/Roaming (CacheScanner's turf), targets logs/dumps/installer-leftovers + out-of-AppData spots (ProgramData/ProgramFiles/WinDir/Documents/LocalLow/drive-roots). `Tests/ScannerOverlapTests` is a REAL-I/O invariant: runs both scanners on the live box and asserts their target trees are disjoint (catches double-count regressions).
- **Steam-aware cleaning (CleanupCategory.Steam):** `Services/Steam/SteamLocator` (HKCU\Software\Valve\Steam\SteamPath, injectable `Func<string?>` seam), `VdfLibraryParser` (pure line parser for `libraryfolders.vdf`), `SteamScanner` (per-library shader/download/temp caches + dumps + logs + user-level htmlcache/CEF). Workshop downloads = Caution (restart if deleted); rest Safe. No process kill — locked files just skip.
- **System Tools page (`SystemToolsPageViewModel` + `SystemActionRunner`, nav "system"):** one-click DISM (analyze[read-only]/component-cleanup/sp-superseded), SFC, WU-cache flush, + Advanced (event-log clear via `wevtutil`, WMI reset via `winmgmt /resetrepository`). Runs external commands, streams stdout/stderr line-by-line via `IProgress<string>`, cancellation kills child. **Dry-run PRINTS the command sequence and launches nothing** (read-only actions run for real regardless); live destructive actions route through the shell confirm modal. **WU flush restarts `wuauserv`/`bits` in a `finally` with `CancellationToken.None`** so a cancelled/failed run never leaves Update stopped.
- **Privacy/MRU (`PrivacyCleaner`, section on the System page):** clears Explorer Recent items (→ Recycle Bin, recoverable) + RunMRU/TypedPaths and 7-Zip FM history (registry, PERMANENT — confirm says so). Registry behind `IRegistryEditor` seam (`HkcuRegistryEditor` real, fake in tests). Dry-run counts, touches nothing.
- **Registry note:** `Microsoft.Win32.Registry` compiles on plain `net10.0` (in the shared framework) — no package/TFM change; guard calls with `OperatingSystem.IsWindows()` for CA1416.
- **Dropped from the script (intentional):** hardcoded personal paths (`D:\Users\Wolf`, `G:\` drives, "Stream" user), process-killing before clean, Steam repair, `cleanmgr`, clock/ARP/AppCompatCache resets, recursive whole-drive `.DS_Store`/`._*` sweeps (only drive-root chkdsk `bootsqm.dat`/`bootTel.dat` via `allDrives`). Dangerous file rules (NuGet full wipe, VS Code `User\History`) carried in as `risky` tier (recycle-bin, never auto-selected).
- **OPEN ITEM:** OneNote 16.0 cache — user asked for it as `risky`, but `CacheScanner` already flags it as `Caution` (same recycle-bin recoverability, same non-auto-select). Kept OUT of the Apps catalog to preserve the disjointness invariant; documented for the user to re-add if they want the explicit Risky label.
- **NOT YET run in the elevated GUI** — build compiles all XAML/compiled-bindings; 189 headless+unit tests cover VM logic, navigation (apps/steam/system tags), scanners, VDF parse, runner streaming/cancel, privacy dry-run. `tools/ui-harness` elevated screenshot pass is the remaining manual step.
